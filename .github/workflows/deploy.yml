name: Deploy to EC2

# main 브랜치에 push하면 실행
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: 코드 가져오기
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: 간단한 배포 시뮬레이션
      - name: Simulate deployment
        run: |
          echo "🚀 배포 시작!"
          echo "📦 코드 푸시됨"
          echo "🐳 Docker 이미지 빌드 (나중에 추가할 예정)"
          echo "✅ 테스트 통과 (나중에 추가할 예정)"
          echo "🚢 EC2에 배포 (나중에 추가할 예정)"
          echo "🎉 배포 완료!"
